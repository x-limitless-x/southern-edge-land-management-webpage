# southern-edge-land-management-webpage

## Facebook Reviews

To display live Facebook reviews, update the `facebookReviewsConfig` object near the bottom of `index.html` with your Page ID, access token, and page URLs. Without those values, the site will fall back to the featured testimonials.
